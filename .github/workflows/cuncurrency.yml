 name: Daily Scheduled Workflow

on:
  push:
    branches:
      - vinayashree
  schedule:
    - cron: '30 3 * * *'

jobs:
  run-daily-task:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a sample task
        run: echo "Good morning! This workflow runs daily at 9 AM IST."


